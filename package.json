{
  "name": "life-of-the-party",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A playful pre-game briefing deck for more intentional nights out.",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/core.js && node --check src/app.js && node --check scripts/build.js",
    "build": "node scripts/build.js",
    "start": "npx serve ."
  },
  "engines": { "node": ">=20" }
}
